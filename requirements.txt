# Core runtime. Install a CUDA build of torch separately for GPU inference:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
transformers>=4.37.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
psutil>=5.9.0

# GPU-only: required for the int8/int4 variants and for device_map="auto".
# Both are no-ops on a CPU-only host but the imports must resolve.
accelerate>=0.24.0
bitsandbytes>=0.41.0; platform_system != "Darwin"
